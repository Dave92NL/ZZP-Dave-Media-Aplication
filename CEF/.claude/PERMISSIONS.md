# đź” PERMISSIONS.md - Access Control List

## đźš« 1. Hard Restrictions
- **File Deletion:** The AI is strictly barred from removing, overwriting, or dropping source modules without manual, single-line confirmation from the user.
- **Global Mutations:** NEVER modify global Git configuration boundaries (\.git/config\) or base OS network interfaces.
- **Library Modification:** Do not add unverified third-party libraries into \package.json\ or \equirements.txt\ without prior architectural authorization.

## đźź˘ 2. Validated Scopes
- Write/Read boundaries limited strictly to execution blocks (\zzp-manager/\, \zzp-mobile/\, \utomation/\).
- Terminal execution allowed only via explicitly requested local PowerShell commands.